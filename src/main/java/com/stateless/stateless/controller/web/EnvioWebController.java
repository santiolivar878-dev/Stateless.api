package com.stateless.stateless.controller.web;

import org.springframework.stereotype.Controller;

@Controller
public class EnvioWebController {

    // Nota: La ruta /account/tracking/{id} queda centralizada en ClientePedidoController.java
    // para evitar colisiones de mapping y el Error 500 al consultar el seguimiento del pedido.

}